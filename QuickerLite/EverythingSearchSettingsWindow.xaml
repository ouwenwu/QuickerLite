<Window x:Class="QuickerLite.EverythingSearchSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Everything搜索设置"
        Icon="Assets/AppIcon.ico"
        Width="560"
        Height="170"
        MinWidth="420"
        MinHeight="150"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Everything.exe 路径"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Foreground="#FF333333"
                   Margin="0,0,0,8"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="PathTextBox"
                     Height="30"
                     Padding="6,3"
                     FontSize="13"/>
            <Button Grid.Column="1"
                    Content="浏览"
                    Width="72"
                    Height="30"
                    Margin="8,0,0,0"
                    Click="BrowseButton_Click"/>
        </Grid>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,14,0,0">
            <Button Content="保存"
                    Width="78"
                    Height="30"
                    Margin="0,0,8,0"
                    Click="SaveButton_Click"/>
            <Button Content="取消"
                    Width="78"
                    Height="30"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
